"use client";

import { ChatInput } from "../app/components";

export default function Page() {
	return (
		<section className="flex flex-wrap gap-4 items-center justify-center w-full h-full border self-center">
			<ChatInput />
		</section>
	);
}
