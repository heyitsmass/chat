import { Fira_Code, Fustat, Geologica, Inter, Outfit, Work_Sans } from "next/font/google";

export const geologica = Geologica({
	//headings
	weight: "variable",
	variable: "--font-geologica",
	display: "swap",
});

export const fustat = Fustat({
	//sans
	weight: "variable",
	variable: "--font-fustat",
	display: "swap",
});

export const fira = Fira_Code({
	// mono
	weight: "variable",
	variable: "--font-fira",
	display: "swap",
});

export const inter = Inter({
	weight: "variable",
	variable: "--font-inter",
	display: "swap",
});

export const outfit = Outfit({
	weight: "variable",
	variable: "--font-outfit",
	display: "swap",
});


export const workSans = Work_Sans({
    weight: 'variable',
    variable: '--font-work-sans',
    display: 'swap'
})